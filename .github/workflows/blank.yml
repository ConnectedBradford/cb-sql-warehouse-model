name: "dbt prd pipeline"

# Triggers
on:
  # Triggers the workflow on push to main branch
  push:
	branches : [main]
 
jobs:
  # Build job
  testing:
	run-on: self-hosted
	steps:
		- run : |
			echo "This works for u"

